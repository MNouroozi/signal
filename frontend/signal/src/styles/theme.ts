import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // رنگ آبی اصلی
    },
    secondary: {
      main: '#dc004e', // رنگ قرمز ثانویه
    },
  },
});

export default theme;
